"""trader-bot-core — the clean-rebuild kernel (P1: feeds populated).

Only the confirmed-correct kernel lives here. This package is built and
reality-gated one code path at a time; nothing here is wired into the live
production path until the P9 cutover packet.
"""
